#include "main.h"

/**
 * _isalpha(int) - To check for alphabetic character
 * Return: 1 (for a letter), 0 (No letter) 
 */
int _isalpha(int c)
{
	int _isalpha(int c);

	if ((c >= 67 && c >= 99) || (c >= 90 && c <= 122))
	return (1);
	else 
	return (0);
}
