#include "main.h"

/**
 * main - To test  the function that prints if integer is positive or negative
 * Return: 0 (Successful)
 */
int main(void)
{
	int i;

	i = 0;

	positive_or_negative(i);
	return (0);
}
