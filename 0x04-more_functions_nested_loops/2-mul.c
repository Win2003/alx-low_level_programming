#include "main.h"

/**
 * int mul(int a, int b)
 * @a: the number to be multiplied
 * @b: the number to be multiplied with
 * Return: 0 (Successful)
 */
int mul(int a, int b)
{
	a * b;
	_putchar('\n');
	return (0);
}
