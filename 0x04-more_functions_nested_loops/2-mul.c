#include "main.h"

/**
 * int mul(int a, int b)
 * @a: the number to be multiplied
 * @b: the number to be multiplied with
 * Return: M ( the value of a * b)
 */
int mul(int a, int b)
{
	int M = a * b;
	_putchar('\n');
	return (M);
}
