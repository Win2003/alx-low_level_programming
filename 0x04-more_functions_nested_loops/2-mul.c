#include "main.h"

/**
 * int mul(int a, int b)
 * @a: the number to be multiplied
 * @b: the number to be multiplied with
 * Return: mul (Successful)
 */
int mul(int a, int b)
{
	mul = a * b;
	_putchar('\n');
	return (mul);
}
